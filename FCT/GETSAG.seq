! ---------------------------------------------------------
! 函数名称: @GETSAG
! 功能描述: 获取指定表面在特定 Y 高度处的绝对矢高。
!           (采用固定坐标采样，适用于 AUT 优化以防震荡)
! 输入参数: ^s (表面编号), ^z (组态编号), ^y (Y坐标绝对高度)
! 返回值:   绝对矢高值
! ---------------------------------------------------------
FCT @GETSAG(NUM ^s, NUM ^z, NUM ^y)

    LCL NUM ^sag  ! 存储矢高计算结果

    ! 调用 SAGF 宏函数计算 (0, ^y) 处真实矢高并取绝对值 
    ^sag == ABSF(SAGF(^s, ^z, 0, ^y))

END FCT ^sag  ! 返回结果